# Checkins base 리소스를 묶는다.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
  - configmap.yaml
  - deployment.yaml
  - hpa.yaml
  - service.yaml
  - ingress.yaml
