# dev 환경 이미지 태그를 덮어쓴다.
# 실제 푸시한 레지스트리/태그로 바꾸고 사용한다.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: doc-converter
spec:
  template:
    spec:
      containers:
        - name: doc-converter
          image: z33hyo/doc-converter:0.3
