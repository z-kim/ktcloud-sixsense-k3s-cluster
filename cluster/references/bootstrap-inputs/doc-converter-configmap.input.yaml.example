# Doc Converter ConfigMap 예시 파일이다.
# 실제 적용 전에는 이 파일을 복사해 doc-converter-configmap.input.yaml 로 만들고
# S3_BUCKET_NAME 값을 채운다.

apiVersion: v1
kind: ConfigMap
metadata:
  name: doc-converter-config
  namespace: apps
data:
  S3_BUCKET_NAME: <terraform output -raw s3_bucket_name 값>
