# 현재는 비활성화된 Checkins DB Secret 예시 파일이다.
# 다시 사용할 때는 이 파일을 활성 경로로 복사해 값을 채운다.

apiVersion: v1
kind: Secret
metadata:
  name: checkins-secret
  namespace: apps
type: Opaque
stringData:
  database-url: mysql://<DB_USERNAME>:<DB_PASSWORD>@<RDS_ENDPOINT>:3306/checkinsdb
